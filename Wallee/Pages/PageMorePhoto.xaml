<Page x:Class="Wallee.Pages.PageMorePhoto"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wallee.Pages"
      xmlns:utils="clr-namespace:Wallee.Utils"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
      Title="PageMorePhoto">
<Page.Resources>
        <utils:ConverterStringLinkToUrl x:Key="ConverterStringLinkToUrl"/>
</Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <ScrollViewer>
        <ItemsControl  ItemsSource="{Binding ListImages}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Image Width="250" Source="{Binding Urls, IsAsync=True, Converter={StaticResource ConverterStringLinkToUrl}}"></Image>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
        </ScrollViewer>
        <Button Grid.Row="1" Click="ButtonBase_OnClickUpdata">Update</Button>
    </Grid>
</Page>
